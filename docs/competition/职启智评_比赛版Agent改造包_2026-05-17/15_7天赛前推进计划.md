# 7 天赛前推进计划

## Day 1：确定主线和文档骨架

目标：

```text
把项目主线从 AI 面试官改成 Career-AgentOS 多智能体就业诊断平台。
```

产出：

```text
docs/competition/PPT主线.md
docs/competition/PPT页面大纲.md
docs/competition/通俗手册_原项目改了什么创新点在哪.md
docs/agents/*.md
```

验收：

```text
能用 3 分钟讲清楚项目是什么、改了什么、创新点在哪。
```

---

## Day 2：生成三岗位沙盘案例

目标：

```text
准备 Python 后端、产品助理、人力资源三个演示案例。
```

产出：

```text
demo_cases/python_backend.json
demo_cases/product_assistant.json
demo_cases/hr_specialist.json
```

Codex 任务：

```text
实现 app/scripts/generate_demo_cases.py
```

验收：

```text
三个案例字段完整，均标记 sample_origin=demo_constructed、for_training=false。
```

---

## Day 3：导出 Agent Trace

目标：

```text
把一次完整多 Agent 工作流变成可截图的 Markdown。
```

产出：

```text
artifacts/agent_trace/python_backend.trace.json
artifacts/agent_trace/python_backend.trace.md
```

Codex 任务：

```text
实现 app/scripts/export_agent_trace.py
```

验收：

```text
trace 包含简历证据、岗位画像、能力缺口、面试追问、报告摘要、Eval。
```

---

## Day 4：生成 Eval Preview

目标：

```text
用评分表证明 Agent 版比普通 Prompt 更聚焦。
```

产出：

```text
artifacts/eval/eval_score_table.csv
artifacts/eval/eval_summary.md
```

Codex 任务：

```text
实现 app/scripts/run_interview_eval_preview.py
```

验收：

```text
有 6 个评分维度，有 base_prompt 和 agent_optimized 对比。
```

---

## Day 5：生成 SFT Preview 和后训练路线图

目标：

```text
展示系统已经具备后训练数据准备能力。
```

产出：

```text
artifacts/sft_preview/train.preview.jsonl
artifacts/sft_preview/summary.preview.json
docs/sft/SFT任务定义.md
docs/roadmaps/SFT_DPO_QLoRA三个月路线.md
```

Codex 任务：

```text
实现 app/scripts/build_sft_preview.py
```

验收：

```text
Preview 文件能打开，messages 格式正确，metadata 有 sample_origin。
```

---

## Day 6：前端展示页 / 截图 / 录屏

目标：

```text
确保现场展示稳定。
```

产出：

```text
Career-AgentOS 技术展示页
演示截图
演示录屏
```

若来不及开发前端：

```text
用 Markdown + 截图 + 录屏替代。
```

验收：

```text
不联网也能讲清楚完整流程。
```

---

## Day 7：PPT 和问答排练

目标：

```text
形成最终答辩材料。
```

产出：

```text
正式 PPT
三分钟讲稿
八分钟讲稿
评委问答库
风险问题回答
```

验收：

```text
能回答：是不是调 API、微调完成没、数据来源、创新点、三个月计划。
```

---

## 每天结束检查

```text
[ ] 今天有没有一个可截图产物？
[ ] 今天有没有一个可放 PPT 的表格/图？
[ ] 今天有没有一个可运行命令或可读文件？
[ ] 今天有没有更新评委问答？
[ ] 今天有没有避免破坏现有演示？
```
