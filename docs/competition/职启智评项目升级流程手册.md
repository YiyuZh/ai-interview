# 职启智评项目升级流程手册

适用项目：`D:\apps\ai-interview`

本手册是项目后续升级的主控路线图。阅读顺序固定为：先看“一页总览”，再看“当前执行看板”，然后按阶段卡逐步执行、验收和记录。

详细历史以根目录 `任务记录文档.md` 为准。本手册只保留当前可执行路线、阶段验收标准和历史索引，不再把每次补充记录堆在主流程中。

## 0. 一页总览

| 项目 | 当前结论 |
|---|---|
| 项目名称 | 职启智评 |
| 工程目录 | `D:\apps\ai-interview`，不改名 |
| GitHub 仓库 | `YiyuZh/ai-interview`，不改名 |
| 服务器目录 | `/opt/apps/ai-interview`，不改名 |
| 当前定位 | 基于生成式 AI、岗位能力图谱和学习任务闭环的就业能力诊断与提升平台 |
| 产品主线 | 简历解析 -> 目标岗位 -> 岗位能力图谱 -> 能力差距诊断 -> 学习任务 -> 模拟面试 -> 训练复盘 |
| 当前唯一主线 | 阶段 91-107 已推送到 `origin/main`；阶段 110/111 已完成学习计划生成闭环本地收口；阶段 112/113 能力匹配纠偏与面试验证专业化收口已完成本地提交 `85c569c`；阶段 115/116 正在补后台资料包预检、草稿和报告导出工具；当前分支比 `origin/main` 超前本地提交，待统一推送 |
| 当前禁止动作 | 服务器未完成迁移和真实闭环前，不再新增大页面、新表、新接口、新模型策略或新演示材料 |
| 当前下一步 | 收口阶段 115/116 后统一推送，再在服务器验证：能力匹配纠偏、学习计划生成、资料包预检页面均可访问；数据库资料仍等人工总结后统一入库 |

### 0.1 当前唯一主线

阶段 91-107 已经完成本地功能实现、收口检查、GitHub 推送、手册状态纠偏和未实现功能优先级梳理。阶段 110/111 已提交并推送 `7f6aedd`，用于修复学习任务 500、后台学习路线为空、学习计划无法生成的问题，并把后台基础路线模板/成熟计划接入前台生成流程。阶段 112/113 已完成本地提交 `85c569c`，用于修复能力匹配死扣字面关键词和面试验证不足的问题。阶段 115/116 是用户明确要求继续开发且不立即上传数据库资料后的功能补齐：后台新增资料包预检页面、浏览器本地草稿和预检报告导出，用于岗位资料正式入库前检查格式、来源和审核状态，不写数据库。当前不再扩大数据库资料内容，而是先收口阶段 115/116，再进入统一验收和缺陷修复：

1. 服务器拉取 `origin/main` 最新提交，并用 `git rev-parse --short HEAD` 记录实际提交号。
2. 执行 Docker 重建、Alembic 迁移、公共岗位画像种子脚本和学习路线初始化，确认 `learning_route_stages` 新字段已生效。
3. 验收用户端、后台端、健康接口、公共岗位画像、学习路线管理、案例标注、测评样本页面。
4. 用 R1 真实简历跑通能力诊断、加入学习任务、学习计划生成、模拟面试、报告和训练复盘。
5. 后台用“案例标注”记录 `case_id`、人工分、证据对齐、问题质量、报告可执行性和数据集归档字段。
6. 人工抽查至少 5 类岗位的学习任务质量：Python 后端、Java 后端、前端、产品、HR/招聘。
7. 如果失败，只修第一条真实错误；不要新增功能绕开验收。

### 0.1.1 未实现功能优先级

| 优先级 | 主题 | 当前判断 | 下一步动作 |
|---|---|---|---|
| P0 | 服务器统一验收与真实闭环 | 最高优先级；当前最大缺口不是页面，而是服务器真实可用证据 | 部署最新 `origin/main`，执行迁移、种子、学习路线初始化和 R1 闭环 |
| P1 | 真实案例与人工评分数据沉淀 | 功能已有工作台，但缺 R1-R5 真实样本和人工评分 | 用案例标注工作台记录 `case_id`、人工分、证据对齐、问题质量和数据集归档 |
| P2 | 岗位知识库内容补全 | 结构已具备，内容质量仍需人工补足 | 优先补 Python、Java、前端、测试、产品、运营、HR 的岗位要求和问答经验 |
| P3 | 学习任务质量人工抽查 | 路线系统已具备，任务是否真能学仍待验证 | 抽查 5 类岗位任务是否有材料、练习、产出物和验收方式 |
| P4 | 材料与手册最终同步 | 必须等真实结果后再更新材料 | 用真实案例替换“待跑测”，同步说明书、演示流程、答辩问答和招募说明 |

### 0.2 升级闸门

后续任何新需求先归类，再决定能不能做。不要把功能补齐、缺陷修复、验收补齐、材料更新混在一个阶段里。

| 闸门类型 | 允许做什么 | 不允许做什么 | 进入条件 |
|---|---|---|---|
| 缺陷修复 | 按日志、报错、截图修具体问题 | 顺手重构或新增功能 | 有明确错误信息或可复现路径 |
| 验收补齐 | 跑构建、迁移、页面、真实案例并记录结果 | 修改业务逻辑来绕开验收 | 当前功能已有实现但缺证据 |
| 功能补齐 | 补缺失的真实产品能力 | 在已有升级包未收口前扩大功能面 | 上一升级包已提交，验收状态清楚 |
| 材料更新 | 更新手册、说明书、PPT、表格 | 把未验收功能写成已完成 | 功能状态和验收结论已明确 |

最高约束：如果当前改动还没有提交、服务器还没有迁移、真实案例还没有跑通，默认只能做“缺陷修复”或“验收补齐”，不能继续开新功能阶段。

### 0.3 禁止继续升级清单

在阶段 99 提交完成后、服务器未部署和未真实验收前，暂时禁止以下动作：

- 禁止新增新的用户端大页面，除非是修复现有页面无法承载主流程。
- 禁止新增新的数据库表或迁移，除非当前持久化表无法完成学习任务和训练复盘闭环。
- 禁止新增新的 `/api/v1/...` 接口，除非现有接口无法表达必要数据。
- 禁止继续扩展更多学习路线，先人工抽查当前多岗位路线是否可执行。
- 禁止继续新增学习路线维护之外的后台模块；阶段 96/97 已经补了路线可维护化和批量质量维护，后续先收口验证。
- 禁止继续生成新 PPT、Word、表格，把材料更新放到真实验收之后。
- 禁止在未清楚 `.codex_tmp/`、`docs/organization_behavior/` 等未跟踪项归属前直接 `git add .`。
- 阶段 110 已属于用户明确要求的“缺陷修复 + 闭环补齐”例外；阶段 110 未收口前，禁止继续新增阶段 111 大功能。

### 0.4 当前未收口项

| 未收口项 | 当前状态 | 收口标准 |
|---|---|---|
| 阶段 91 学习任务与训练复盘持久化 | 已在阶段 95 提交；服务器迁移和真实页面验收待补 | 服务器执行迁移后，任务和复盘跨刷新、跨浏览器仍保留 |
| 阶段 92 学习路线与进度统计 | 已在阶段 95 提交；服务器迁移和真实页面验收待补 | Python 后端任务能显示路线来源、阶段、类型、耗时，筛选和统计正确 |
| 阶段 93 多岗位路线 | 已在阶段 95 提交；服务器迁移和真实页面验收待补 | 技术岗和非技术岗任务都具体可执行，非技术岗不再出现“小 Demo”口径 |
| 阶段 96 学习路线后台维护 | 阶段 99 已提交并推送；服务器迁移和页面验收待补 | 后台可维护路线阶段；学习计划生成优先使用数据库启用路线，数据库无匹配时回退内置路线 |
| 阶段 97 学习路线质量审核与批量维护 | 阶段 99 已提交并推送；服务器迁移和页面验收待补 | 后台能查看质量总览、导入导出 JSON、复制路线、预览学习任务生成效果 |
| 阶段 98 学习路线覆盖矩阵与启用质量护栏 | 阶段 99 已提交并推送；服务器迁移和页面验收待补 | 后台能按岗位查看路线覆盖率、能力项匹配路线和低质量路线启用提示 |
| 阶段 110/111 学习计划生成闭环与 500 修复收口 | 已提交并推送到 `origin/main`，提交为 `7f6aedd`；服务器 Alembic 迁移和真实页面验收待补 | 学习任务接口返回可读迁移错误；后台路线区分基础模板和成熟计划；前台可生成、编辑并保存学习计划 |
| 阶段 112 能力匹配纠偏与面试验证优先 | 本地初版已实现并纳入提交 `85c569c`，待服务器真实简历验收 | SQL/MySQL/PostgreSQL、TypeScript/JavaScript、Spring/JVM、Postman/Pytest 等同义能力不再被误判完全缺失；“熟悉 Redis”但无项目证据会标为声明待验证；模拟面试优先追问岗位必需和待验证能力 |
| 阶段 113 能力匹配纠偏专业化收口 | 本地验证通过并纳入提交 `85c569c`，待推送和服务器验收 | 统一前端证据状态工具，补齐多岗位别名测试，确认 `verification_target` 能进入问答数据和后续报告/后台记录 |
| 阶段 115/116 资料包预检工具 | 本地初版实施中，不写数据库，待后台构建和人工验收 | 后台可载入/粘贴岗位资料包 JSON，预检岗位要求、问答经验、能力模型、面试追问、来源说明和审核状态，支持浏览器本地草稿、预检报告导出和可复制 Markdown 预览 |
| 阶段 117 RAG 多信号路由与幻觉约束 | 本地实施中，不改接口和数据库，待后端专项测试和服务器验收 | 在岗位知识切片排序中增加多信号评分头、证据置信度和低置信警告；明确这是检索与提示层约束，不宣称修改外部大模型 Transformer 内部注意力机制 |
| 阶段 118 五人维护成员资料夹与入库预检规范 | 资料协作规范已建立，待成员投放资料与人工审核 | 新增 `docs/competition/成员资料/`，按五人维护角色收集资料；所有资料先进入 `待检查/`，登记来源、岗位、能力标签、审核状态和建议入库位置，审核后再决定是否入库 |
| 阶段 119 完整面试闭环案例模板与角色4标注口径 | 完整案例采集口径已固化，待角色4按模板填写 R1-R5 | 完整真实案例必须包含简历、能力诊断、至少 3-5 轮问答、报告、训练复盘和人工评分；只有简历的样本只能标为“静态简历诊断样本” |
| 阶段 120 成员资料体系收口与完整案例采集规范固化 | 资料类型枚举、登记表示例和阶段状态已收口，不改代码和数据库，待精准提交 | 统一资料类型：完整闭环样本、静态诊断样本、重复运行记录、岗位资料、问答经验、学习路线、答辩材料、服务器验收记录；后续资料先预检再入库 |
| 阶段 122 基于岗位评分逻辑的简历润色页面 | 本地初版已实施，待后端编译、专项测试、前端构建和真实 PDF 验收 | 用户上传 PDF 后复用现有简历解析与评分，再调用大模型生成不编造经历的分段润色建议和 Markdown 润色稿 |
| 阶段 101/102 面试问答经验库 | 本地初版和收口验证完成；服务器验收待补 | 前台私有岗位画像和后台公共岗位画像都能维护结构化面经，切片能按审核状态参与追问 |
| 阶段 104 案例标注工作台 | 本地初版已验证；服务器验收待补 | 后台能集中查看 R1-R5 真实案例、人工评分、AI 核查信号和数据集归档状态 |
| 阶段 105 阶段 103/104 收口提交 | 已提交并推送到 `origin/main`，提交为 `891e46e`；服务器统一验收待补 | 阶段 103/104 文件已精准纳入 Git，未混入构建产物和无关资料 |
| 阶段 106 主流程手册状态纠偏 | 已提交并推送到 `origin/main`，提交为 `0944ff7`；服务器统一验收待补 | 手册已纠偏为阶段 91-106 后只做统一验收和必要缺陷修复 |
| 阶段 107 服务器统一验收与 R1 闭环 | 优先级和执行入口已记录，待在服务器执行 | 服务器报告 `result=PASS`，R1 跑通并写入真实闭环记录 |
| 阶段 1/13 服务器真实运行验收 | 待补 | 服务器报告 `result=PASS`，用户端、后台端、健康接口和关键页面可访问 |
| 阶段 9/10/20/22 真实主流程 | 待补 | R1 真实简历跑通能力诊断、学习任务、面试、报告、训练复盘 |
| 阶段 11/12 真实样本和人工评分 | 待补 | 至少 5 个完整案例、5 条人工评分、1 个案例 3 次重复运行 |
| 工作区收口 | 阶段 99 已完成本地预检、精准提交并推送到 `origin/main` | 服务器部署后，进入真实闭环验收 |

### 0.5 下一步推荐顺序

1. 工作区收口检查：`git status --short`，先确认未跟踪项是否属于本次升级包。
2. 本地预检：后端编译、关键 pytest、用户端构建、后台端构建、`git diff --check`。
3. Git 提交与推送：阶段 91-98 已提交为 `7347ede`；阶段 101/102 已提交为 `58b3225`；阶段 103/104/105 已提交为 `891e46e`；阶段 106 已提交为 `0944ff7`。
4. 服务器迁移部署：拉取 `origin/main` 最新代码、重建服务、执行 Alembic、执行岗位画像种子脚本。
5. 真实案例闭环：用 R1 跑通主流程，并写入真实闭环 CSV。
6. 人工任务质量验收：抽查不同岗位学习任务是否能指导真实学习和产出。
7. 内容补全：只在 P0/P1 通过后，再补岗位知识库、问答经验和材料同步。

### 0.6 无效升级识别规则

- 如果没有真实案例输入，不继续优化评分结论和学习建议文案。
- 如果服务器未执行迁移，不继续讨论线上学习任务丢失，先迁移和查表。
- 如果学习任务没有人工抽查，不继续扩展更多岗位路线。
- 如果当前改动未提交，不继续做大规模重构。
- 如果只是入口重复、页面职责重复，优先合并入口，不新增页面。
- 如果问题来自具体日志或截图，按缺陷修复处理，不把它扩展成新阶段。

### 0.7 成员资料收集区

团队资料统一放在 `docs/competition/成员资料/`。该目录用于收集五位维护成员的岗位资料、面经、案例标注、学习路线、答辩材料和服务器验收记录。

使用规则：

1. 每位成员只把资料先放入本人角色目录的 `待检查/`。
2. 同步填写该角色目录下的 `资料登记表.csv`。
3. 审核状态统一使用 `可入库 / 仅参考 / 待核验 / 不采用`。
4. 只有移动到 `已检查可入库/` 的资料，后续才允许进入岗位画像、问答经验、能力模型、面试追问、学习路线、案例标注或答辩材料。
5. 大型文件、视频、压缩包不直接放入仓库；只记录链接、摘要和来源说明。

## 1. 当前执行看板

### 1.1 已完成并可作为基础

| 模块 | 当前状态 | 说明 |
|---|---|---|
| 项目边界 | 已确认 | 继续保留 `ai-interview` 目录、仓库和服务器路径 |
| 多岗位生态 | 已纠偏 | 技术岗、产品岗、运营岗、HR 岗、行政岗继续共存 |
| 岗位画像 | 方向通过，代码已有 12 个画像 | 仍需补真实问答经验、公司侧重点和后台种子验收 |
| 能力图谱 | 已实施初版 | 后续用真实岗位人工审核能力项、等级和权重 |
| 结构化知识库 | 已实施初版 | 后续验收后台四分区编辑、保存和切片重建 |

### 1.2 实施初版待收口

| 升级包 | 包含阶段 | 当前状态 | 下一步 |
|---|---:|---|---|
| 学习任务与学习路线系统升级包 | 91/92/93/96/97/98/99/100/110/111 | 阶段 111 已提交并推送 `7f6aedd`；服务器迁移和真实页面仍未验收 | 下一步在服务器执行 Alembic、学习路线初始化、真实页面闭环和人工任务质量验收 |
| 能力匹配与面试验证纠偏 | 112/113 | 阶段 112/113 已完成本地实现、验证和提交 `85c569c`：多岗位关键词别名归一、证据强度分层、声明待验证、面试追问路由、前端状态工具统一；当前待推送和服务器真实简历验收 | 用真实简历验证 SQL/数据库、Redis/缓存、后端接口、前端 TypeScript/JavaScript、Java Spring/JVM、测试 Postman/Pytest、数据分析 Excel/BI 等能力不再错判，并确认面试题能解释验证目标 |
| 岗位资料入库准备 | 115/116 | 资料包预检后台页面本地初版实施中；当前只校验、保存本地草稿、导出报告和预览 Markdown，不写数据库 | 后续岗位资料、面经、公司侧重点和能力模型总结完成后，先通过资料包预检，再决定是否批量入库或人工粘贴到公共岗位画像 |
| 面试问答经验库升级包 | 101/102 | 前台/后台结构化面经维护本地初版已完成，本地验证通过；不新增接口和数据库 | 后续随阶段 100/统一服务器验收一并检查 |
| 真实案例与人工标注工作台 | 104/105 | 后台案例标注工作台本地初版已验证；阶段 105 已精准提交并推送到 `origin/main`；复用面试记录和 `training_sample_review`，不新增数据库和路由 | 后续与 R1-R5 真实案例、人工评分和评测样本导出一起验收 |
| 能力诊断与训练闭环 | 20/22 | 页面初版完成，真实简历闭环未验收 | 用 R1 跑通诊断、学习任务、面试、报告、复盘 |
| 后台知识库维护 | 21 | 页面初版完成，后台真实编辑未验收 | 检查四分区编辑、旧格式保留、切片重建 |

### 1.3 必须人工确认

| 阶段 | 人工检查什么 | 过关效果 |
|---:|---|---|
| 1/13 | 服务器、用户端、后台端能真实访问 | 页面不是只在本地构建通过 |
| 3 | 岗位画像是否真实、合格、可用于面试 | 画像不是空泛模板 |
| 17 | 能力项、等级、权重是否合理 | 能解释能力差距从哪里来 |
| 18 | 岗位要求、问答经验、能力模型、追问规则是否可维护 | 后台能管理知识资产 |
| 19 | 学习任务是否具体、能执行 | 用户知道下一步学什么、做什么 |
| 20 | 能力诊断是否准确反映简历缺口 | 不再出现“证据明显不足但显示无差距” |
| 22 | 训练复盘是否能串起学习和面试结果 | 能形成持续提升闭环 |

### 1.4 暂缓项

| 暂缓项 | 暂缓原因 | 重新启动条件 |
|---|---|---|
| 新增大页面 | 当前页面已经够多，先证明主闭环可用 | 真实案例证明现有入口无法承载 |
| 新增数据库结构 | 阶段 91 迁移尚未服务器验收 | 现有 `learning_tasks`、`training_reviews` 无法满足明确需求 |
| 继续扩展学习路线 | 阶段 93 已覆盖主要岗位，但未人工抽查质量 | 当前路线抽查通过后再扩展 |
| 新 PPT/Word/表格 | 材料必须跟随已验收系统状态 | 服务器验收和 R1-R5 数据补齐后 |

## 2. 下一步推荐路线

### 2.1 运行验收

目标：确认服务器上的真实系统可以跑，而不是只在代码层通过。

要做：

1. 本地执行 `powershell -ExecutionPolicy Bypass -File scripts\stage79_local_preflight.ps1`。
2. 服务器部署最新代码。
3. 执行数据库迁移。
4. 执行公共岗位画像种子脚本。
5. 检查用户端、后台端、健康接口。
6. 检查公共岗位画像、测评样本、面试记录页面。

阶段 90 最新记录：

- 2026-05-10 已在本地执行完整预检：后端编译、关键 pytest、用户端构建、后台构建、真实闭环 CSV 普通校验均通过。
- 当前缺口是服务器未生成 `docs/competition/server_validation_reports/stage79_server_verify_latest.md`，因此阶段 1/13 仍不能标记完成。
- 服务器报告 `result=PASS` 后，才进入 R1 真实简历闭环。

过关后进入：真实简历主流程验收。

### 2.2 真实案例闭环

目标：用一份真实简历验证系统主线。

要做：

1. 上传真实简历。
2. 选择目标岗位。
3. 查看能力诊断。
4. 把缺失能力加入学习任务。
5. 开始模拟面试。
6. 至少完成 3 轮问答，推荐 5 轮；每轮记录系统问题、验证能力、用户回答、AI 评价和人工评价。
7. 生成报告。
8. 进入训练复盘。

完整真实案例的口径：必须同时包含简历、能力诊断、面试问答、报告、训练复盘和人工评分。只有简历和能力诊断、尚未跑面试问答的资料，只能登记为“静态简历诊断样本”，不能写成完整真实案例。

过关后进入：真实样本和人工评分沉淀。

### 2.3 数据沉淀

目标：让项目不只是页面能跑，还能拿出验证数据。

要做：

1. 使用 `真实案例闭环验收记录.md` 和 `datasets/真实闭环验收记录模板.csv`。
2. 准备 5-10 个真实或公开授权案例。
3. 每个完整案例记录目标岗位、简历证据、能力差距、学习任务、3-5 轮面试问答、报告结论和训练复盘。
4. 人工评分至少覆盖技术岗和非技术岗。
5. 同一案例重复运行 3 次，记录评分波动。
6. 使用 `python scripts\append_real_closed_loop_record.py --case-id R1 --set 字段=值` 追加标准记录。
7. 使用 `python scripts\validate_real_closed_loop_records.py` 检查记录完整性。
8. 真实数据补齐后，使用 `python scripts\validate_real_closed_loop_records.py --strict` 检查阶段 11/12 是否达到验收线。

过关后进入：服务器持久化验收和材料更新。

### 2.4 服务器持久化

目标：让学习任务和训练复盘成为账号级数据，不再只依赖浏览器 `localStorage`。

当前状态：阶段 91 已实施服务器持久化初版；阶段 92 已实施 Python 后端路线化学习任务初版；阶段 93 已实施多岗位学习路线初版；阶段 96 已实施学习路线后台维护初版；阶段 97 已实施学习路线质量审核与批量维护初版；阶段 98 已实施学习路线覆盖矩阵与启用质量护栏初版；阶段 99 已完成本地预检和精准提交准备。下一步不再新增功能，转入服务器统一验收和真实任务质量验收。

已落地内容：

1. 新增 `learning_tasks` 表，按 `user_id + task_key` 去重保存任务、笔记、完成状态和薄弱项变化。
2. 新增 `training_reviews` 表，按 `user_id + interview_id` 保存自评分、复盘笔记和下一轮目标。
3. 新增 `/api/v1/learning-tasks` 和 `/api/v1/training-reviews` 客户端接口。
4. 用户端学习任务页默认读取服务器；旧版本地任务可一键迁移到账号。
5. JSON 导入导出保留为备份和迁移功能，导入后写入服务器。
6. 学习任务接入项目内置学习路线常量，不依赖 `E:\学习` 运行时路径。
7. Python 后端岗位任务优先匹配 Python 基础、Web API、数据库 ORM、FastAPI、Redis/Docker/Celery、项目复盘等路线阶段。
8. `learning_tasks` 兼容新增可选字段：`route_source`、`route_stage`、`task_type`、`estimated_minutes`、`due_date`、`task_metadata`。
9. 学习任务页新增路线阶段、能力项、完成状态筛选，并展示总任务、完成率、路线阶段数和预计剩余学习时长。
10. Java、前端、测试、算法、数据分析、产品、运营、新媒体、HR、招聘、行政等岗位已补多岗位路线初版。
11. 非技术岗任务生成改为案例分析、文档产出、流程复盘、数据复盘、情景演练等口径，不再统一写成 Demo。
12. 新增 `learning_route_stages` 表和后台 `/api/v1/backoffice/learning-routes` 管理接口，支持学习路线阶段的新建、编辑、启用/停用、删除。
13. 后台新增“学习路线管理”页面，可查看路线总数、启用数、岗位覆盖数、质量检查提示，并按岗位、类别、任务类型筛选。
14. 学习计划生成逻辑改为数据库启用路线优先；数据库无匹配时回退内置路线，仍无匹配时回退通用模板。
15. 后台学习路线管理新增质量总览、质量等级、JSON 导入导出、复制路线和任务生成预览；导入导出使用 `learning_routes_v1`。
16. 后台学习路线管理新增岗位覆盖矩阵、岗位能力到路线的匹配预览，以及启用“不建议启用”路线前的质量风险确认。

后续验收重点：

1. 执行 Alembic 迁移后确认两张表存在。
2. 报告页和能力诊断页加入任务后，刷新页面仍能看到。
3. 换浏览器或清缓存后登录同一账号，学习任务仍存在。
4. 训练复盘保存后刷新仍保留。
5. Python 后端岗位生成的任务能看到路线来源、路线阶段、任务类型和预计耗时。
6. 学习任务页筛选和进度统计与任务列表一致。
7. 技术岗和非技术岗都不会回落到空泛通用模板。
8. 非技术岗任务不出现“小 Demo”式技术练习文案。
9. 后台学习路线管理能完成 CRUD，且停用路线不会再被学习计划匹配。
10. 路线质量提示能帮助人工发现空泛任务，至少包括练习任务、验收方式、预计耗时和关键词。
11. 后台能导出当前路线 JSON，导入同版本 JSON 后按 `job_id + category + route_stage + task_type` 去重更新。
12. 后台能复制路线为默认停用的新路线，避免复制后立刻影响学习计划生成。
13. 后台任务生成预览能展示路线会生成的任务标题、练习任务、验收方式和预计耗时。
14. 后台覆盖矩阵能展示 12 个岗位的能力项数量、启用路线数、完整路线数、覆盖率和状态。
15. 选择岗位后能看到每个能力项匹配到的路线；无匹配时明确显示“待补路线”。

### 2.5 材料更新

目标：让项目说明、答辩材料、演示脚本与当前系统一致。

要做：

1. 更新项目说明书。
2. 更新答辩问答。
3. 更新演示流程检查表。
4. 更新测试案例与人工评分对比。
5. 必要时重新生成 PPT、Word、表格。
6. 面向新成员更新项目背景与成员招募说明。

## 3. 阶段 0-22 顺序执行卡

### 阶段 0：项目边界确认

目标：保证升级不影响 Git、服务器和部署路径。

当前状态：已完成，后续持续遵守。

操作步骤：

```powershell
cd D:\apps\ai-interview
git remote -v
git status --short
```

过关标准：

- 本地目录仍是 `D:\apps\ai-interview`。
- GitHub 仓库仍是 `YiyuZh/ai-interview`。
- 服务器目录仍是 `/opt/apps/ai-interview`。
- 后端接口前缀仍是 `/api/v1/...`。

不通过处理：发现新目录、新仓库、新部署路径时立即停止，先改回原边界。

下一阶段：阶段 1。

### 阶段 1：基线可运行检查

目标：确认后端、用户端、后台端真实可运行。

当前状态：部分完成；2026-05-10 阶段 90 本地完整预检已通过，包含后端编译、关键测试、用户端构建、后台端构建和真实闭环 CSV 普通校验；服务器真实运行验收待补。

操作步骤：

```powershell
cd D:\apps\ai-interview
powershell -ExecutionPolicy Bypass -File scripts\stage79_local_preflight.ps1
```

服务器报告回收后：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\stage79_local_preflight.ps1 -ValidateServerReport
```

真实闭环数据补齐后：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\stage79_local_preflight.ps1 -StrictClosedLoopRecords
```

手动检查：

```powershell
cd D:\apps\ai-interview\ai-interview-backend
python -m compileall app

cd D:\apps\ai-interview\ai-interview-frontend
npm run build -- --outDir .vite-build-check

cd D:\apps\ai-interview\ai-interview-admin
npm run build -- --outDir .vite-build-check
```

服务器验收见本手册第 4 节。

过关标准：

- 后端编译通过。
- 用户端构建通过。
- 后台端构建通过。
- 服务器健康接口可访问。
- 用户端和后台端页面可打开。

不通过处理：先看构建日志和 `docker compose logs --tail=300 app` 中第一条异常。

下一阶段：阶段 2，或补阶段 13。

### 阶段 2：多岗位生态纠偏

目标：保留原系统多岗位能力，不收窄成人力资源岗位项目。

当前状态：已完成。

操作步骤：

- 检查页面文案不排斥技术岗。
- 默认岗位保持通用技术岗示例。
- 保留用户自由输入任意目标岗位。

过关标准：

- Python 后端、Java 后端、前端开发、测试、算法、数据分析、产品、运营、HR、行政均可作为目标岗位。
- 技术岗不会被强行拉回 HR 语境。

不通过处理：发现页面文案把系统写成单岗位项目时立即改回多岗位口径。

下一阶段：阶段 3。

### 阶段 3：岗位画像库扩展

目标：形成技术岗和非技术岗都可用的公共岗位画像。

当前状态：方向通过，待细化与数据库种子验收。

操作步骤：

- 人工审核 `docs/competition/阶段3岗位画像候选稿.md`。
- 确认 12 个岗位画像方向可用。
- 服务器执行岗位画像种子脚本。
- 后台检查画像数量、启用数、切片数。

过关标准：

- 后台公共岗位画像不少于 12 个。
- 技术岗和非技术岗都存在。
- Python 后端等技术岗包含语言、数据库、缓存、网络、并发、系统设计、项目追问等重点。

不通过处理：画像为空先执行种子脚本；内容空泛则回到候选稿补充。

下一阶段：阶段 4。

### 阶段 4：数据集与题库建设

目标：明确数据集是什么、来源是什么、哪些内容能入库。

当前状态：初版完成，真实样本待补。

操作步骤：

- 使用 `数据集与岗位知识库建设手册.md`。
- 补数据源清单。
- 补岗位面试题库样例。
- 标记来源许可和人工判断。

过关标准：

- 数据来源可追溯。
- 不复制来源不明面经。
- 有技术岗和非技术岗题目样例。

不通过处理：来源不清的内容只放待核验，不进入正式知识库。

下一阶段：阶段 5。

### 阶段 5：技术岗八股与知识切片

目标：让技术岗面试能围绕岗位知识追问，而不是泛泛聊天。

当前状态：初版完成，切片效果待服务器确认。

操作步骤：

- 检查技术岗画像中的知识点。
- 执行切片重建。
- 后台查看切片来源和内容。

过关标准：

- 技术岗切片能覆盖语言基础、数据库、Redis、HTTP、并发、算法、系统设计、项目追问。
- 切片能被面试追问使用。

不通过处理：切片为空先查种子和重建接口；切片混乱则调整 Markdown 分区标题。

下一阶段：阶段 6。

### 阶段 6：Prompt 与证据约束

目标：让简历分析、面试追问和报告都基于证据，不编造经历。

当前状态：已有基础。

操作步骤：

- 用真实简历检查解析结果。
- 检查报告是否区分“已有证据”和“需要追问”。
- 检查面试题是否围绕缺口追问。

过关标准：

- 简历没写的技能不能直接判定为已掌握。
- 报告能指出证据不足。
- 面试能把缺口转成追问。

不通过处理：发现编造经历时先修 Prompt 和结构化解析兜底。

下一阶段：阶段 7。

### 阶段 7：轻量机器学习匹配

目标：形成可解释的关键词、TF-IDF、规则评分和最终分。

当前状态：已完成初版。

操作步骤：

- 上传简历。
- 查看匹配指标。
- 比较不同岗位下的匹配变化。

过关标准：

- `keyword_coverage`、`semantic_score`、`rule_score`、`final_score` 可见或可追溯。
- 技术岗和非技术岗分数逻辑不同。

不通过处理：分数明显不合理时记录案例，后续用人工评分校准权重。

下一阶段：阶段 8。

### 阶段 8：Embedding 可选增强

目标：在不破坏 TF-IDF 回退的前提下增加语义向量匹配。

当前状态：实施初版，待真实 API 验收。

操作步骤：

- 配置有效 DeepSeek 或 OpenAI API Key。
- 配置 embedding 模型。
- 上传简历触发分析。
- 检查语义来源显示。

过关标准：

- embedding 成功时能显示实际 provider 和 model。
- embedding 失败时自动回退 TF-IDF。
- 不因 embedding 失败中断简历分析。

不通过处理：先检查 API Key、base URL、模型名和日志中的 embedding 错误码。

下一阶段：阶段 9。

### 阶段 9：简历分析主流程升级

目标：让简历分析输出岗位匹配、证据、能力缺口和学习入口。

当前状态：部分完成。

操作步骤：

- 上传真实简历。
- 选择目标岗位。
- 查看简历分析结果。
- 检查能力差距和学习计划。

过关标准：

- 分析结果能说明简历支持什么结论。
- 能指出岗位缺口。
- 不把缺少结构化数据写成“没有明显能力差距”。

不通过处理：如果 AI JSON 异常，先查解析兜底；如果能力差距为空，查 `ability_gap_profile` 和前端兜底卡片。

下一阶段：阶段 10 或阶段 20。

### 阶段 10：模拟面试闭环升级

目标：让用户能从岗位画像和简历缺口进入 3-5 轮模拟面试。

当前状态：部分完成。

操作步骤：

- 在简历分析后点击开始模拟面试。
- 回答至少 1-2 轮。
- 查看是否生成面试记录。
- 结束后查看报告。

过关标准：

- 能进入面试页。
- 问题与岗位画像、简历证据、能力缺口相关。
- 后台面试记录能看到新记录。

不通过处理：开始面试失败时查看 `/api/v1/interviews/start` 附近第一条日志异常。

下一阶段：阶段 11 或阶段 22。

### 阶段 11：后台样本与人工评分

目标：让后台能支撑人工评分对比和验证数据导出。

当前状态：部分完成，真实样本待补。

操作步骤：

- 后台打开测评样本页。
- 检查空态或样本统计。
- 完成真实面试后进行人工标注；完整样本必须能追溯到 3-5 轮问答。
- 导出验证数据。

过关标准：

- 无样本时不显示服务器内部错误。
- 有样本时能看到统计。
- 至少形成技术岗和非技术岗人工评分记录。
- 每个被称为“完整案例”的样本必须包含简历、问答、报告和人工评分；只有简历的样本只算静态诊断样本。

不通过处理：500 时查 `evaluation-datasets` 日志和数据库迁移状态。

下一阶段：阶段 12。

### 阶段 12：测试案例与稳定性

目标：形成 5-10 个可复查案例和重复测试记录。

当前状态：初版完成；2026-05-10 已补真实闭环验收记录模板，真实重复测试待补。

操作步骤：

- 准备 5-10 个案例。
- 按 `真实案例闭环验收记录.md` 记录单案例闭环。
- 完整案例至少包含 3 轮问答，推荐 5 轮；低于 3 轮只能标记为不完整样本。
- 按 `datasets/真实闭环验收记录模板.csv` 批量登记案例状态。
- 执行 `python scripts\validate_real_closed_loop_records.py` 检查 CSV。
- 每个案例重复运行 3 次。
- 记录模型、参数、岗位、分数、人工评价、波动。

过关标准：

- 案例覆盖技术岗和非技术岗。
- R1-R5 中标为完整案例的记录都包含面试问答过程。
- 评分波动有记录。
- 人工评分与 AI 评分能对比。
- 严格检查 `python scripts\validate_real_closed_loop_records.py --strict` 通过。

不通过处理：案例不足时不要写成“验证完成”，只标记为“样本积累中”。

下一阶段：阶段 13。

### 阶段 13：构建、浏览器、接口验收

目标：交付前确认代码、页面、接口都能跑。

当前状态：部分完成；2026-05-10 本地检查已通过，服务器真实验收待补。

操作步骤：

```powershell
cd D:\apps\ai-interview\ai-interview-backend
python -m compileall app

cd D:\apps\ai-interview\ai-interview-frontend
npm run build -- --outDir .vite-build-check

cd D:\apps\ai-interview\ai-interview-admin
npm run build -- --outDir .vite-build-check
```

过关标准：

- 后端、用户端、后台端检查通过。
- 浏览器能跑完整主流程。
- 后台公共岗位画像、面试记录、测评样本可用。

不通过处理：前端构建看 Vite 报错；后端看 Python traceback；服务器看 Docker 日志。

下一阶段：阶段 14。

### 阶段 14：GitHub 与服务器发布

目标：按原仓库和原服务器路径发布。

当前状态：阶段 91-107 已分批提交并推送；服务器统一验收待补。旧的 `git add .` 发布方式已废弃。

操作步骤：

```powershell
cd D:\apps\ai-interview
git status --short
# 如有本阶段明确改动，只精准暂存相关文件；禁止使用 git add .
git add <本阶段明确需要提交的文件>
git commit -m "<本阶段提交信息>"
git push origin main
```

服务器：

```bash
cd /opt/apps/ai-interview
git fetch origin
git reset --hard origin/main
git rev-parse --short HEAD
docker compose up -d --build app frontend admin
docker compose exec app alembic upgrade head
docker compose exec app python -m app.scripts.seed_competition_knowledge_bases
bash scripts/stage79_server_verify.sh --deploy
python scripts/validate_server_validation_report.py --report docs/competition/server_validation_reports/stage79_server_verify_latest.md
```

过关标准：

- GitHub 有最新提交，且本地提交没有混入构建产物或无关资料。
- 服务器能访问用户端和后台端。
- 数据库迁移和种子脚本执行成功。
- 自动验收报告 `result=PASS`。

不通过处理：不要改目录名；先复制第一条部署或日志异常继续定位。

下一阶段：阶段 15 或继续真实验收。

### 阶段 15：答辩材料收口

目标：让说明书、PPT、Word、表格与系统当前能力一致。

当前状态：部分完成。

操作步骤：

- 更新项目说明书。
- 更新演示流程检查表。
- 更新答辩问答库。
- 更新测试案例与人工评分对比。
- 必要时重新生成 PPT、DOCX、XLSX。

过关标准：

- 材料不写已经不存在的页面。
- 不夸大自研模型、真实微调、企业合作。
- 能讲清能力诊断与提升闭环。

不通过处理：材料与系统不一致时，以系统真实能力为准改材料。

下一阶段：按比赛或课程要求提交。

### 阶段 16：评委反馈归档与项目定位纠偏

目标：把项目从单一模拟面试工具升级为就业能力诊断与提升平台。

当前状态：已完成文档纠偏，待持续落到页面和材料。

操作步骤：

- 阅读 `评委点评整理_正式版.md`。
- 确认主线：能力诊断、岗位能力图谱、学习任务、训练复盘。
- 后续页面和材料都按新主线表达。

过关标准：

- 不再把项目只讲成 AI 面试。
- 能说清岗位目标、个人能力、能力差距、提升路径、训练复盘。

不通过处理：发现页面或材料仍像普通 AI 聊天工具时，优先改信息架构。

下一阶段：阶段 17。

### 阶段 17：岗位能力图谱与能力差距模型

目标：让系统能解释用户与目标岗位的能力差距从哪里来。

当前状态：实施初版，待人工审核和服务器验收。

操作步骤：

- 检查岗位能力项。
- 检查等级、权重、关键词、证据线索、可提升系数。
- 用真实简历查看能力差距输出。

过关标准：

- 每个重点岗位至少有 6 个能力项。
- 能力差距能对应简历证据。
- 优先级不是空泛排序。

不通过处理：能力项空泛时先人工修画像；差距缺失时查 `ability_gap_profile`。

下一阶段：阶段 18。

### 阶段 18：岗位知识库结构化改造

目标：把岗位画像升级为可维护的结构化岗位知识库。

当前状态：实施初版，待人工审核和运行验收。

操作步骤：

- 后台编辑公共岗位画像。
- 检查四分区：岗位要求、问答经验、能力模型、面试追问。
- 保存后重建切片。
- 查看切片分类。

过关标准：

- 四分区内容能保存和回填。
- 旧格式内容不会丢失。
- 切片能识别不同分区。

不通过处理：保存失败查后台接口；切片分类失败查 Markdown 标题。

下一阶段：阶段 19。

### 阶段 19：能力提升系统与学习进度

目标：把能力差距转成可执行学习任务，并记录进度。

当前状态：学习任务中心已实施服务器持久化、多岗位路线化任务、后台学习路线维护、质量审核与批量维护初版；阶段 110 已补学习计划生成接口、后台基础模板/成熟计划字段、前台生成计划草稿编辑和学习任务 500 可读错误，待提交、服务器迁移、后台页面验收和真实任务质量验收。

操作步骤：

- 从报告或能力诊断中把缺失能力加入学习任务。
- 打开 `/learning-tasks`。
- 首次进入时如检测到旧版本地任务，点击“迁移到账号”。
- 按路线阶段、能力项、完成状态筛选任务。
- 查看总任务、完成率、路线阶段完成情况和预计剩余学习时长。
- 勾选完成、填写笔记、记录薄弱项变化。
- 导出和导入学习任务 JSON，确认导入后写回服务器。
- 后台打开“学习路线管理”，检查路线阶段能按岗位、类别和任务类型维护；生成学习计划时优先读取启用的数据库路线。

过关标准：

- 任务有能力项、目标岗位、来源报告或简历。
- 任务能执行，不只是泛泛建议。
- Python 后端岗位任务能落到 Python 基础、FastAPI、数据库、Redis/Docker/Celery、项目复盘等明确路线阶段。
- Java、前端、测试、算法、数据分析、产品、运营、新媒体、HR、招聘、行政等岗位也能落到各自路线阶段。
- 非技术岗任务以案例、文档、流程、数据复盘或情景演练为主，不再套用技术 Demo 文案。
- 每个路线化任务能显示路线来源、阶段、任务类型和预计耗时。
- 阶段筛选、能力筛选、完成状态筛选可用，进度统计不与列表矛盾。
- 后台学习路线管理能查看总数、启用数、岗位覆盖数，并能新建、编辑、启用/停用、删除路线阶段。
- 学习路线质量提示能指出缺少练习任务、验收方式、预计耗时或关键词。
- 数据库启用路线优先于内置路线；停用路线不会参与匹配；数据库无匹配时仍能回退内置路线和通用模板。
- 刷新后进度仍保留。
- 换浏览器或清空本地缓存后，登录同一账号仍能读取任务。
- 导入导出可恢复，且不会重复生成同一 `task_key`。

不通过处理：任务太空泛时先查后台学习路线管理中的练习任务和验收方式，再查内置学习路线常量和学习计划生成逻辑；路线字段为空时查 `learning_route_stages`、`app.constants.learning_routes`、`analysis.learning_plan` 和 `/api/v1/learning-tasks` 返回；保存失败时先查 `/api/v1/learning-tasks`、`/api/v1/backoffice/learning-routes`、`learning_tasks` / `learning_route_stages` 表和 Alembic 迁移状态；旧本地数据只作为迁移备份，不再作为正式数据源。

下一阶段：阶段 20。

### 阶段 20：用户端能力诊断页面

目标：让用户独立查看能力差距、证据说明和提升入口。

当前状态：实施初版，待真实简历验收。

操作步骤：

- 打开 `/ability-diagnosis`。
- 指定或自动读取最近完成简历。
- 查看匹配分、能力差距、证据说明。
- 把缺失项加入学习任务。

过关标准：

- 没有结构化差距时显示“暂无结构化能力差距数据”，不能误写“没有明显差距”。
- 有证据缺口时能生成兜底差距卡片。
- 文案使用“当前简历 / 这份简历 / 当前用户”，避免像招聘裁决。

不通过处理：低匹配简历显示无差距时，优先修前端兜底逻辑和后端 `ability_gap_profile`。

下一阶段：阶段 21 或阶段 22。

### 阶段 21：后台岗位知识库维护页面

目标：让后台能真实维护岗位知识资产。

当前状态：实施初版，待人工审核和后台运行验收。

操作步骤：

- 打开后台公共岗位画像。
- 新建或编辑画像。
- 填写四个分区。
- 保存并重建切片。

过关标准：

- 后台名称为“职启智评管理后台”，不再使用“参赛后台管理”等比赛口径。
- 页面风格简洁，黑灰白为主。
- 四分区完整度、问答经验数量、来源审核状态能提示维护质量。

不通过处理：发现比赛话术继续残留时统一改成真实项目口径。

下一阶段：阶段 22。

### 阶段 22：训练闭环与复盘报告

目标：把能力诊断、学习任务、模拟面试和报告串成持续训练闭环。

当前状态：训练复盘已实施服务器持久化初版，待真实训练闭环验收。

操作步骤：

- 完成一次模拟面试。
- 打开 `/training-review`。
- 选择已完成面试。
- 查看能力差距、学习任务完成情况、面试结果、下一轮建议。
- 填写自评、复盘笔记、下一轮目标。

过关标准：

- 无已完成面试时有清晰空状态。
- 有已完成面试时能生成复盘摘要。
- 能读取学习任务完成数和未完成任务。
- 复盘笔记刷新后仍保留。
- 换浏览器或清空本地缓存后，登录同一账号仍能读取对应面试复盘。
- 报告接口失败时，仍允许保存手动复盘记录。

不通过处理：无面试记录先查 `/interviews`；报告读取失败查 `getReport`；复盘保存失败查 `/api/v1/training-reviews/{interview_id}`、`training_reviews` 表和面试归属权限。

下一阶段：回到阶段 11/12 做真实数据沉淀，或继续补服务器统一验收。

## 4. 服务器验收命令

服务器部署：

```bash
cd /opt/apps/ai-interview
git fetch origin
git reset --hard origin/main
docker compose up -d --build app frontend admin
docker compose restart app frontend admin
```

推荐自动执行部署与验收：

```bash
cd /opt/apps/ai-interview
git fetch origin
git reset --hard origin/main
bash scripts/stage79_server_verify.sh --deploy
```

脚本默认生成验收报告：

```text
docs/competition/server_validation_reports/stage79_server_verify_latest.md
```

如需保留多次验收记录，可以指定报告文件：

```bash
bash scripts/stage79_server_verify.sh --deploy --report docs/competition/server_validation_reports/$(date +%Y%m%d_%H%M%S)_stage79.md
```

过关标准：脚本结尾显示 `PASS: server validation checks passed.`，报告中的“自动结论”为 `PASS`，公共岗位画像不少于 12，知识切片大于 0，且最近关键日志没有新的首要 Traceback。

报告检查：

```bash
python scripts/validate_server_validation_report.py --report docs/competition/server_validation_reports/stage79_server_verify_latest.md
```

过关标准：输出 `result=PASS`。如果输出 `ERROR`，先处理第一条错误；如果只有 `WARN`，结合后端最近日志判断是否是历史残留。

阶段 90 执行提醒：

- 本地已经执行 `powershell -ExecutionPolicy Bypass -File scripts\stage79_local_preflight.ps1`，结果为 PASS。
- 当前仓库没有 `docs/competition/server_validation_reports/stage79_server_verify_latest.md`，所以本地无法替服务器判断 PASS。
- 服务器执行自动验收后，把报告文件提交或复制回本地，再运行：

```powershell
cd D:\apps\ai-interview
powershell -ExecutionPolicy Bypass -File scripts\stage79_local_preflight.ps1 -ValidateServerReport
```

服务器报告通过后，执行 R1 闭环并追加 CSV：

```powershell
python scripts\append_real_closed_loop_record.py --case-id R1 --set resume_source=2590603008詹已誉简历.pdf --set target_position=产品助理/产品经理实习生 --set environment=server --set git_commit=<服务器当前commit> --set upload_status=成功 --set ability_diagnosis_status=成功 --set learning_tasks_added=1 --set interview_start_status=成功 --set report_status=成功 --set training_review_status=成功 --set admin_interview_record=成功 --set evaluation_dataset_status=空态 --set status=部分通过
python scripts\validate_real_closed_loop_records.py
```

只检查当前服务器、不重建：

```bash
cd /opt/apps/ai-interview
bash scripts/stage79_server_verify.sh
```

数据库迁移：

```bash
docker compose exec app alembic current
docker compose exec app alembic heads
docker compose exec app alembic upgrade head
```

公共岗位画像种子：

```bash
docker compose exec app python -m app.scripts.seed_competition_knowledge_bases
```

数据库检查：

```bash
docker compose exec postgres sh -lc 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "select scope, count(*) from position_knowledge_bases group by scope;"'
docker compose exec postgres sh -lc 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "select count(*) as slice_count from position_knowledge_base_slices;"'
```

日志检查：

```bash
docker compose logs --tail=300 app
docker compose logs --tail=300 app | grep -Ei "Traceback|UndefinedColumn|relation .* does not exist|interviews/start|evaluation-datasets"
```

Redis/Celery 配置检查：

```bash
grep -E "^(REDIS_HOST|REDIS_PORT|REDIS_PASSWORD|CELERY_BROKER_URL|CELERY_RESULT_BACKEND)=" .env
```

建议：`REDIS_HOST=redis`、`REDIS_PORT=6379`。如果手写的 `CELERY_BROKER_URL` 把密码误放到端口位置，应先修 `.env`。

## 5. 人工验收清单

用户端：

- 能登录。
- 能上传简历。
- 能选择或输入目标岗位。
- 能查看能力诊断。
- 能把缺失能力加入学习任务。
- 能开始模拟面试。
- 能生成报告。
- 能进入训练复盘。

后台端：

- 公共岗位画像数量不少于 12。
- 能编辑四分区知识库。
- 能保存并重建切片。
- 面试记录能看到新建记录。
- 测评样本无数据时显示空态，不显示红色 500。

真实案例：

- 至少 1 个技术岗案例。
- 至少 1 个非技术岗案例。
- 每个案例记录简历证据、目标岗位、能力差距、学习任务、面试表现、报告结论。
- 后续扩展到 5-10 个案例。

材料：

- 项目说明书与当前系统一致。
- PPT 不写内部边界话术。
- 答辩问答能说明：系统不是简单调用 AI，而是岗位知识库、能力模型、差距诊断、学习任务和训练复盘的闭环。

## 6. 历史记录索引

详细历史不再放进本手册正文。需要追溯时查看 `任务记录文档.md`：

| 阶段 | 内容 |
|---:|---|
| 48-56 | 职启智评参赛化、流程手册、岗位画像、报名表 |
| 57-60 | 初赛材料、真实案例、阶段 3 收口 |
| 61 | DeepSeek 优先 embedding 可选增强 |
| 64-70 | 仓库瘦身、后台 500、开始面试 500、简历解析兜底、运行验收准备 |
| 71 | 初赛后评委反馈理解与路线重构 |
| 72 | 岗位能力图谱与结构化知识库第一轮 |
| 73 | 能力提升系统与学习进度第一轮 |
| 74 | 用户端能力诊断主页面第一轮 |
| 75 | 后台岗位知识库分区维护第一轮 |
| 76 | 学习任务中心、能力诊断修正与后台真实项目化 |
| 77 | 训练闭环与复盘报告第一轮 |
| 78 | 升级流程手册结构重构 |
| 79 | 服务器真实运行验收与真实简历闭环启动，本地收口检查和 GitHub 推送已完成，服务器验收待补 |
| 80 | 真实案例闭环验收记录体系，模板已补，真实服务器跑测待执行 |
| 81 | 服务器阶段 79 自动验收脚本，脚本已补，等待服务器执行 |
| 82 | 真实闭环记录检查器，脚本已补，当前 CSV 为待跑测状态 |
| 83 | 本地阶段 79 预检脚本，脚本已补并通过一次完整预检 |
| 84 | 服务器验收报告输出增强，脚本可生成 Markdown 留痕报告，等待服务器执行 |
| 85 | 服务器验收报告检查器，脚本已补，可将服务器报告转成 PASS/FAIL 结论 |
| 86 | 本地预检接入服务器报告检查，可用 `-ValidateServerReport` 将服务器报告纳入统一预检 |
| 87 | 真实闭环 CSV 记录追加工具，脚本已补，可按案例追加标准跑测记录 |
| 88 | 真实闭环 CSV 严格验收门槛，脚本已补，可检查案例数、完整闭环、人工评分和重复运行 |
| 89 | 本地预检接入真实闭环严格检查，可用 `-StrictClosedLoopRecords` 将样本达标纳入统一预检 |
| 90 | 服务器真实验收与首个真实简历闭环启动；本地完整预检已通过，服务器报告与 R1 真实闭环待执行 |
| 91 | 学习任务与训练复盘服务器持久化初版；新增两张表、客户端接口和服务器优先读写，待统一服务器验收 |
| 92 | 学习路线资源接入与任务进度增强；新增项目内置学习路线常量、路线字段、阶段筛选和进度统计，待统一服务器验收 |
| 93 | 多岗位学习路线补齐与任务质量增强；Java、前端、测试、算法、数据、产品、运营、HR、行政等岗位路线初版已补，待人工任务质量验收 |
| 94 | 升级流程手册防中断、防冗余重构；新增升级闸门、禁止继续升级清单、当前未收口项和无效升级识别规则 |
| 95 | 学习任务系统升级包收口与提交准备；本地预检、专项测试和精准提交范围确认已完成，待服务器迁移部署 |
| 96 | 学习路线后台维护与任务模板质量升级；新增学习路线表、后台 CRUD 接口、管理页面和数据库路线优先匹配逻辑，待统一提交、迁移和服务器验收 |
| 97 | 学习路线质量审核与批量维护升级；新增质量总览、质量等级、JSON 导入导出、复制路线和任务生成预览，待统一提交、迁移和服务器验收 |
| 98 | 学习路线覆盖矩阵与启用质量护栏；新增岗位覆盖矩阵、能力项路线匹配预览和低质量路线启用确认，待统一提交、迁移和服务器验收 |
| 99 | 学习任务与学习路线系统升级包收口；本地预检和专项测试通过，精准提交阶段 91-98 相关文件，服务器迁移和真实案例验收待阶段 100 |
| 100 | 服务器迁移与真实闭环统一验收启动；本地已推送 `origin/main`，当前环境无法直连服务器，服务器部署、Alembic 迁移和 R1 真实闭环待人工在服务器执行 |
| 101 | 前后台统一的面试问答经验库；前台私有画像和后台公共画像统一结构化面经编辑，后端切片按审核状态参与追问，本地测试通过，待服务器验收 |
| 102 | 面试问答经验库收口与部署准备；复跑后端切片测试、用户端构建和后台端构建，精准提交阶段 101 改动，不使用 `git add .` |
| 103 | 项目背景与成员招募说明；新增面向新成员的项目背景、技术流程、当前进度、成员角色和近期任务说明 |
| 104 | 后台真实案例标注工作台；集中维护 R1-R5、人工评分、AI 输出核查和数据集归档信号，复用现有面试记录与标注接口 |
| 105 | 阶段 103/104 收口提交与部署准备；复跑后端编译、人工标注扩展测试、评测样本导出回归和后台构建，精准提交并推送，服务器统一验收待补 |
| 106 | 主流程手册状态纠偏；将顶部主线更新为阶段 91-105 已推送、下一步只做服务器统一验收和真实闭环，不再按旧提交阶段继续执行 |
| 107 | 未实现功能优先级梳理与下一步升级计划落地；明确 P0-P4 优先级，修正过期提交号和阶段 14 的 `git add .` 命令，下一步进入服务器统一验收 |
| 108 | 成员招募说明改为五人维护分工版；从招募角色转为当前五人维护角色和交接说明 |
| 109 | 五人维护分工二次均衡；岗位资料拆为技术岗和非技术岗，运行验收归技术负责人，数据标注职责细化到字段和表格 |
| 110 | 学习计划生成闭环与 500 修复；补学习任务接口可读数据库错误、学习路线模板/成熟计划字段、学习计划 options/generate 接口和前台生成计划草稿编辑，待提交与服务器迁移验收 |
| 111 | 阶段 110 收口提交与服务器迁移验收准备；复跑本地验证、清理临时构建产物、精准暂存并提交推送，服务器 Alembic 与真实页面验收待执行 |
| 112 | 能力匹配纠偏与面试验证优先；新增多岗位关键词别名归一、证据强度分层、声明待验证和面试追问路由，本地提交为 `85c569c` |
| 113 | 能力匹配纠偏专业化收口；统一前端证据状态工具、补齐多岗位别名测试、确认 `verification_target` 进入问答数据，待推送和服务器真实简历验收 |
| 114 | 能力匹配收口状态纠偏与推送准备；修正手册中“阶段 113 待提交”的滞后状态，明确当前分支比 `origin/main` 超前 1 个提交 |
| 115 | 后台资料包预检工具；支持岗位资料包 JSON 模板、格式预检、来源审核检查和 Markdown 预览，不写数据库，等待资料总结后统一入库 |
| 116 | 资料包预检草稿与报告导出；支持浏览器本地保存/恢复/清空资料包草稿，并导出包含错误、提醒、分区完整度和面经审核统计的预检报告 |
| 117 | RAG 多信号路由与幻觉约束；在岗位知识切片排序中新增多信号评分头、证据置信度和低置信警告，并强化 Prompt 证据边界，不宣称修改外部大模型内部注意力机制 |
| 118 | 五人维护成员资料夹与入库预检规范；新增成员资料收集区、公共提交规范、五个角色资料登记表和四类审核状态目录 |
| 119 | 完整面试闭环案例模板与角色4标注口径；区分静态简历诊断样本和完整面试闭环样本，补 R1 完整记录模板、静态诊断模板和重复运行模板 |
| 120 | 成员资料体系收口与完整案例采集规范固化；统一资料类型枚举、登记表示例和完整案例采集口径，待后续成员投放资料后人工审核 |
| 121 | 人工智能创新赛申报书重构；基于评委点评、申报表模板和当前系统状态，生成申报书 Markdown 长版与 DOCX 正式版，重点回应选题需求、市场缺陷、产品空白、内容功能和创新点 |
| 122 | 基于岗位评分逻辑的简历润色页面；新增用户端 `/resume-polish`，复用简历解析、岗位评分、能力差距和岗位画像上下文调用大模型生成证据约束润色建议，结果写入 `analysis.resume_polish`，待真实简历验收 |

后续每次升级都必须同步：

1. `任务记录文档.md` 追加阶段记录。
2. 先检查本手册“升级闸门”和“当前未收口项”，确认是否允许继续新增功能。
3. 本手册“当前执行看板”更新状态。
4. 对应阶段卡更新当前状态、过关标准和下一阶段。
5. 如涉及演示或答辩，再更新 `职启智评操作手册.md`、项目说明书、演示检查表或 PPT。
