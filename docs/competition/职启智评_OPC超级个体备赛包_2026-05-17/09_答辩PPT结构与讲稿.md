# 答辩 PPT 结构与讲稿

## 1. PPT 标题

```text
职启智评 OPC：一个人 + AI Agents 的高校就业能力诊断与训练服务单元
```

副标题：

```text
基于 Career-AgentOS 的 AI 原生工作流、人机协同结构与轻量化创新实践
```

## 2. 推荐 12 页结构

### 第 1 页：我是谁，不是传统团队

讲：

```text
我是 OPC 超级个体，用 AI Agents 扩展个人能力，构建高校就业服务轻量化创新单元。
```

### 第 2 页：行业问题

```text
大学生不了解岗位要求
简历证据不足
面试训练零散
就业老师难以高频覆盖所有学生
```

### 第 3 页：我的 AI 原生工作流

展示：

```text
简历解析 -> 岗位画像 -> 证据评分 -> 能力差距 -> 简历润色 -> 学习任务 -> AI 面试 -> 报告复盘 -> 人工评分 -> 样本沉淀
```

### 第 4 页：人机协同结构

展示人和 AI 分工。

### 第 5 页：OPC 如何替代小团队

展示“小团队角色 -> AI Agent 替代方式”表格。

### 第 6 页：Career-AgentOS 多 Agent 架构

展示 13 个 Agent。

### 第 7 页：项目原型已经可运行

展示职启智评用户端、后台端和部署结构。

### 第 8 页：三岗位演示 Trace

展示 Python 后端、产品助理、人力资源专员。

### 第 9 页：Eval Preview 和 SFT Preview

讲：

```text
当前是 Preview 和 SFT-ready 数据闭环，不冒充真实微调结果。
```

### 第 10 页：复赛集中限时实测能力

展示你如何现场构建 AI 工作流。

### 第 11 页：三个月补实路线

展示真实样本、人工评分、SFT、场景试点。

### 第 12 页：总结

```text
职启智评的核心不是单个 AI 面试官，而是我作为 OPC 设计并运行的一套 AI 原生就业服务工作流。
```

## 3. 三分钟讲稿

```text
各位评委好，我的项目叫职启智评 OPC。它不是传统意义上“我做了一个 AI 面试网站”，而是我作为一个超级个体，围绕高校就业服务场景，设计的一套 AI 原生工作流。

大学生求职中常见的问题是：不了解岗位真实能力要求，简历经历不能转化成岗位证据，模拟面试训练不连续，反馈也难以形成下一步学习任务。传统就业指导依赖老师面对面咨询，质量高但覆盖频次有限。

我的解决方案是把就业指导拆成多个 AI Agent：简历证据 Agent 判断能力是否有证据，岗位画像 Agent 提供岗位要求和面经切片，Gap Agent 分析能力差距，简历润色 Agent 在不造假的前提下优化表达，面试官 Agent 围绕缺口追问，学习任务 Agent 生成后续训练计划，Eval Agent 和数据治理 Agent 负责评分、授权和样本沉淀。

在这个结构中，AI 负责高频文本处理、检索、生成和初步评分；我作为 OPC 负责人负责目标选择、资料审核、隐私授权、评分标准和最终判断。通过 Codex、ChatGPT 和大模型 API，我一个人可以完成原本需要产品、研发、就业顾问、测试和材料团队共同完成的工作。

目前项目已经形成 Vue 用户端、Vue 管理端、FastAPI 后端、PostgreSQL、Redis/Celery 和 Docker 部署原型，具备简历解析、能力诊断、简历润色、学习任务、AI 面试、报告复盘、后台岗位画像和案例标注等能力。下一步三个月，我会补齐真实授权案例、人工评分、OpenAI SFT 和高校场景小范围验证。

所以，职启智评的核心价值不是一个单点 AI 工具，而是一个人 + AI Agents 构建的高校就业服务轻量化创新单元。
```

## 4. 八分钟讲稿结构

```text
1 分钟：OPC 主体介绍
1 分钟：高校就业场景痛点
1.5 分钟：AI 原生工作流
1.5 分钟：人机协同结构
1 分钟：可运行原型和三岗位演示
1 分钟：Eval / SFT-ready 数据闭环
1 分钟：三个月补实路线和总结
```
